{
  "name": "pixeldrain-bypasser",
  "version": "1.0.0",
  "description": "Cloudflare Worker to bypass PixelDrain rate limits",
  "main": "worker.js",
  "scripts": {
    "build": "echo 'No build step required'",
    "deploy": "wrangler deploy worker.js --name pixeldrain-bypasser",
    "dev": "wrangler dev worker.js",
    "test": "echo \"No tests specified\""
  },
  "keywords": ["cloudflare", "worker", "pixeldrain", "bypass"],
  "author": "sh13y",
  "license": "MIT",
  "devDependencies": {
    "wrangler": "^3.0.0"
  }
}
